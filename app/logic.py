# app/logic.py

def calculate_crf_pages(num_pages: int, factor: float) -> float:
    """
    Example placeholder for your CRF logic.
    Replace this with your actual Excel formula(s).
    """
    if num_pages > 0:
        return num_pages * factor
    else:
        return 0.0
